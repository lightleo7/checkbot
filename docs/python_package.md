Чтобы сделать пакет python(для адекватных импортов) нужно в каждой папке, которая будет модулем сделать файл __init__.py и в нём импортировать всё то, что будет использоваться из модуля. 
Дальше нужно перейти в client и написать uv pip install -e . и всё, пакет готов.
